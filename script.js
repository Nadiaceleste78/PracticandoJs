let numeros = [1,2,3];

let suma=0;

function sumarElementos (numeros) {
  
  if (suma = numero + suma) {

    mensaje.innerHTML = `<li> La suma de los números es : ${suma} .</li>`

  }
}
  for (numero of numeros){

    sumarElementos (numero)

  }

