// *********************
// EJERCICIO 1
// *********************

// Dado un array de números enteros, devolver la suma de todos los elementos.

// Pistas:
// 1 - Los números ya son de tipo number, así que no es necesario parsearlos
// 2 - Es necesario usar un ciclo que nos permita iterar sobre cada item del array

function sumarElementos(numeros) {
  let suma = 0;
  
  let mensaje = document.querySelector("#mensaje");
  // Generar la lógica acá
  
  for (let i = 0; i <numeros.length; i++);

    if (suma += numeros [i]);{
      
    console.log( `La suma de los números es: ${suma}`)}

  return suma;
}

let numeros = [1, 2, 3];

// Ejecutamos la función e imprimimos el resultado en la consola.
//console.log(sumarElementos(numeros));
